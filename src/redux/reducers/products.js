const INITIAL_STATE = {
    data : []
}

const ProductsReducer = (state = INITIAL_STATE, action) => {
    switch(action.type) {
        default :
            return state
    }
}

export default ProductsReducer